{
  "name": "designsystemin",
  "private": true,
  "scripts": {
    "ds:build": "tsx scripts/build-foundations.ts",
    "test": "vitest run",
    "dev": "pnpm --filter playground dev",
    "build": "pnpm ds:build && pnpm --filter playground build"
  }
}
