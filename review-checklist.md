# 생성 화면 검토 체크리스트 — 디자이너용 한 장

생성된 HTML 을 받으면 이것만 본다. 규칙 위반은 기계가 잡는다. 사람은 판단만 한다.

## 1. 스펙 섹션 맨 위 5줄 (파일을 열면 "스펙" 뷰에 있다)

```
DS 갱신일            2026-09-21          ← 오늘과 멀면 오래된 DS 로 만든 것
DS 밖 요소            0개                 ← 0 이 아니면 아래 목록 + 배너가 있어야 한다
원문까지 읽은 컴포넌트  0개                 ← 0 이 아니면 그 컴포넌트에 스토리가 없다 = 티켓
값 채운 컨트롤         N개 · 조건부 확인됨   ← 검색창·필터 칩·입력에 값을 넣었으면 ✕ 같은 조건부 UI 가 있어야 한다
프로그레스            포함/제외 · 이유       ← 진행률이 실제로 있는 화면만 포함
```

위 두 줄이 0 이면 기계 층은 통과한 것이다. 코드를 열 필요가 없다.

## 2. 사람만 할 수 있는 판단

- **컬럼·필드 구성** — 이 화면에 이 정보가 맞나. 빠진 것, 과한 것.
- **문구** — 제목·라벨·상태 이름이 제품 용어와 맞나.
- **정보 위계** — 먼저 보여야 할 것이 먼저 보이나.
- **프로그레스 판단** — 스펙에 적힌 이유가 납득되나.
- **DS 밖 요소** — 목록의 각 항목이 진짜 새 컴포넌트인가, 스토리만 없는 것인가(30분 일), 아니면 기존 컴포넌트로 될 일인가.

## 3. 어디로 보내나

| 스펙 섹션 상태 | 처리 |
|---|---|
| DS 밖 요소 0 · 원문 읽음 0 | 요청자가 엔지니어에게 바로 보낸다 |
| DS 밖 요소 있음 | 스펙 섹션의 DES 티켓 문안으로 디자이너에게 |
| 원문 읽음 있음 | 스토리 누락 — 디자이너가 스토리 추가(반나절 이내) |

## 4. 보지 않아도 되는 것

클래스가 DS 안인가, 임의 색값이 있나, 상태 4개가 다 있나, 알약과 섹션이 맞나 — 전부 자가 검사와 배포 감사가 본다.
