// MissingMark — 「미입력」 표기 요소 잠금 (2026-09-07 · 2026-09-08 개정)
// - 이탤릭 "미입력"(secondary-foreground)만. 빨강 점은 2026-09-08 디자이너 확정으로 뺐다 —
//   상세·모달에서는 값을 바로 채우는 자리라 경고 점 없이도 뜻이 통하고, 빨강은 오류로 읽힌다.
// - 쓰는 곳(빈 값 규칙, 2026-09-08): 상세 페이지·모달 = 이 표기 / 목록 표 = 흐린 대시(—).
//   목록은 훑는 면이라 빈 칸을 전부 대시로 두고, 채우는 면(상세·모달)에서만 "아직 안 들어옴"을 말한다.
//   예) 계약 상세 배정 호선 금액 · 금액 수정 모달 파생값/합산 · 호선 상세 IMO/선명/납품 예정일
// - 값이 "아직 안 올라온 게 정상"인 칸(도면 등)에는 쓰지 않는다 — 그건 누락이 아니라 진행 중이다.
// - 종전엔 계약 호선 목록 파일 안에만 있었다. 두 벌로 갈라지기 전에 공용 부품으로 뺐다([[person.tsx]]와 같은 처리).

export function MissingMark() {
  return <span className="text-sm italic text-secondary-foreground">미입력</span>;
}
