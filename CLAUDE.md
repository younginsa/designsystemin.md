# CLAUDE.md — 이 저장소에서의 작업 절차

## 절대 규칙 (모든 작업에 우선)

1. **모든 응답은 4단계 블록으로 시작한다.** QUESTIONS → OPINION(3~5줄) → PLAN → WAIT.
   예외 없음 — 지시가 아무리 구체적이어도, 오타 수정이어도 블록을 생략하지 않는다.
   질문은 텍스트가 아니라 AskUserQuestion 모달로 묻는다. 모달 답변은 실행 승인이 아니다.
   WAIT에서 멈춘다. 실행 승인은 딱 두 가지뿐이다 — 사용자가 직접 입력한 "go",
   또는 모달에서 승인 선택지를 고른 경우. 그 외에는 무엇도 승인으로 간주하지 않는다.
   지시가 아무리 명확해도 "지시 = go" 해석은 금지.

2. **사용자 메시지에 질문이 있으면 어떤 작업보다 먼저 직접 답한다.**
   답을 실행 보고서 중간에 묻어버리지 않는다. 질문 답변이 끝나기 전에는
   코드·파일 작업을 시작하지 않는다.

3. **세션 첫 응답의 첫 줄에 SessionStart 훅의 [365 DS] 상태 줄을 그대로 표시한다.**
   (예: `[365 DS] 이미 최신 상태 ✓`) 훅 메시지를 받지 못했으면
   `[365 DS] 상태 미수신 — git pull 권장`이라고 쓴다.

4. **요소 단위 수정 원칙 — 개별 패치 금지.** 특정 요소(상태 표기·칩·버튼·표 등)에
   수정 요청이 오면, 지적된 그 한 곳만 고치지 않는다. 같은 요소가 쓰인 곳을
   저장소 전체에서 전수 검사하고, 수정이 적용될 항목과 위치(파일:줄)를 리스트로
   먼저 보고한 뒤, 컨펌을 받고 전체를 한 번에 수정한다. 요소 단위로 잠그고(lock)
   움직여야 개별 수정 요청이 반복돼도 통일성·일관성이 유지된다.
   같은 요소의 스펙(크기·색·간격)이 화면마다 갈라지는 순간, 그것은 수정이 아니라 파손이다.

5. **"진행할까요?" 종결 원칙 — 임의 코딩 금지.** 수정·생성으로 이어질 수 있는
   모든 응답(진단·검토·질문 답변·제안)은 반드시 "진행할까요?"로 끝내고 멈춘다.
   사용자가 그 질문에 명시적으로 답한 경우에만 코딩을 시작한다.
   질문형·검토형 요청("왜 이래?", "확인해줘", "어떻게 생각해?")은 그 자체로 실행
   승인이 아니다 — 보고까지만 한다. 진단을 보고한 턴에 이어서 바로 코딩하는 것도 금지.
   모르겠으면 추측으로 코딩하지 말고 물어본다.

AI 친화 디자인 시스템 저장소다.
설계서: docs/superpowers/specs/2026-07-29-ai-design-system-design.md

## 페이지 생성 요청을 받으면 (순서 고정 — 세션 직접 생성 계약)

0. **최신화** — 세션 시작 시 SessionStart 훅(`.claude/settings.json`)이 자동으로
   `git pull` 한다. 장수 세션은 입력 시점에 마지막 동기화 후 1시간이 지나 있으면
   훅이 자동 재동기화한다. 그래도 생성 직전에는 `git pull` 1회를 직접 실행한다
   (원격 최신 DS·채택 목록 반영). pull 결과에 CLAUDE.md 변경이 포함되어 있으면
   CLAUDE.md를 다시 읽고 새 절차를 따른다.
   **pull에 approved.json·ds365.json·CLAUDE.md·components/ 변경이 포함되면** 새 항목의
   노트까지 읽고, 기존 생성 화면(`playground/app/generated/`)에 소급 적용 대상이 있는지
   1회 스캔해 보고한다 — 적용 자체는 요청자 승인 후.
   훅 pull 요약의 "N files changed" 총계와 나열된 파일 수를 대조한다(산수만 — 추가
   명령 없음). 불일치하면 `git diff --stat`으로 전체 목록을 직접 확인한 뒤 소급 스캔한다.
   pull에 package.json·pnpm-lock.yaml 변경이 포함되면 `pnpm install`을 1회 실행하고 진행한다.
1. **규정 검토** — `regulations/`에서 해당 제품 파일 확인. 충돌·주의사항이 있으면
   생성 전에 알리고 확인받는다. 파일이 없으면 한 줄 보고 후 진행.
2. **규칙·어휘 로드** — `design.md` 전체 + `playground/public/approved.json`(채택 어휘)
   + `playground/public/ds365.json`(365 토큰·조정값)을 읽는다.
   **사용 가능 어휘 = approved.json 채택분뿐이다(유일한 게이트).** ds365.json은
   토큰·값 조정·재현 제작 상태의 기록이며 어휘를 부여하지 않는다 —
   재현 컴포넌트도 채택되어야 어휘다.
   어휘 밖 컴포넌트는 만들지 않는다 — 회색 점선 자리표시 + "미채택: <이름>" 라벨로
   자리만 잡고 보고한다. 채택 목록이 비어 있으면 생성 불가로 답한다.
   **어휘는 레시피보다 우선한다** — 레시피가 요구하는 요소라도 어휘 밖이면
   쓰지 말고 생성 전에 질문한다.
3. **프레임 대조 + ask-first** — `layout/`의 **레이아웃 프레임**과 대조한다.
   레시피는 페이지 유형이 아니라 프레임(셸·영역 구조)이다. 기본 질문은 하나 —
   "이 화면은 어느 프레임의 **본문**인가, 아니면 프레임 없는 **단독 화면**인가."
   맞으면 어떤 프레임으로 가는지 선언하고 그 관례(셸·영역·전폭)를 상속한다.
   본문 유형이 뚜렷하면 `layout/body-patterns.md`의 문자(A 리스트·B 상세·C 위저드·D 대시보드)도
   함께 선언한다 — 예) "① 프레임 + C 본문". 고정 요소는 본문 컬럼을 벗어나지 않는다.
   애매하면(특히 와이어프레임·신규 레이아웃) **생성 전에 AskUserQuestion으로
   확인한다.** 선택지는 `layout/README.md` 목록 순서의 **번호(① ② …)** +
   "단독 화면(프레임 없음)" + "어디에도 없음(질문 계속)"으로 제시하고,
   질문에 템플릿 그림 링크를 함께 준다 — 배포 365 페이지 `/index.html#templates`
   (로컬: `http://localhost:3003/index.html#templates`). "어디에도 없음"이면
   앱 셸 포함 여부·전폭·와이어프레임의 색 글씨·주석 해석을 이어서 확인한다.
   추측으로 레이아웃을 발명하지 않는다.
   **입력에 앱 셸이 포함되어 있으면**(제품 HTML·풀페이지 스크린샷) 생성 전에 묻는다 —
   "① 입력의 셸 그대로 재현(실물 스냅샷 목적) ② 프레임 셸 + 입력의 본문만(DS 통일 목적)".
   어느 쪽이든 **어휘 게이트는 동일 적용** — 셸을 재현할 때도 미채택 요소는
   자리표시 + DS 요청 제안.
   **AskUserQuestion을 쓸 수 없는 환경(don't-ask 모드 등)이면 같은 질문을
   텍스트로 묻고, 답을 받기 전에는 생성을 시작하지 않는다.**
4. **생성 — 뼈대 우선, 단일 파일 진화** — `playground/app/generated/<이름>/page.tsx` 하나를
   만들고 끝까지 그 파일만 다듬는다. 첫 산출은 레이아웃·컴포넌트 배치 위주의 **뼈대**
   (최대치 실행 금지). 이후 수정 요청은 **재생성 없이 해당 부분만 수술**한다.
   모든 화면은 **기본·빈·로딩·에러** 4개 상태를 포함한다(추가 상태는 요청 시).
   상태 전환 UI는 `@ds/ui`의 **`StatePreview` 프리셋**(상단 중앙 플로팅 필)을 쓴다 —
   본문·헤더에 임베드하지 않는다.
   **산출물의 목적은 화면 이해(시각 스펙)다** — 인터랙션·마이크로 디테일도 요청 시에만.
5. **self-check** — 임의 값이 없는지 확인:
   `grep -nE '#[0-9a-fA-F]{3,6}|-\[' playground/app/generated/<이름>/page.tsx` → 결과 없어야 함.
   제품 규정 파일에 self-check 게이트가 있으면 함께 적용한다 — 예: Control 화면은
   `regulations/control.md`의 시거리 타이포 게이트(지정 시거리 스케일 세트 미만 크기 차단).
   **가독성 게이트**: 생성물의 글자×면 조합(text-*/bg-*)은 `dstk/contrast-pairs.json`
   허용 목록 안이어야 한다 — 미달·미선언 조합은 **자동 치환 금지**, 차단 + 알림 +
   가장 가까운 통과 토큰 제안까지만 한다.
   **틴트 게이트**: `/N` 투명도 변형은 같은 파일의 `tints.allowed` 목록 안만 허용 —
   목록 밖은 차단 + 통과 틴트 제안. (레거시 유물 `/generated/update-image-download`는
   감사 제외 — 재제작 시 은퇴 예정.)
   **전체 빌드·타입체크 금지** — `pnpm build`·`tsc` 같은 무거운 검증을 돌리지 않는다.
   self-check와 dev 서버 렌더 확인이면 충분하다.
   **예외(관리자 저장소): `components/src`를 고친 커밋 전에는
   `pnpm --filter playground exec tsc --noEmit` 1회를 실행한다** — dev 서버가 눈감는
   타입 에러가 Vercel 배포를 끊는 것 방지(2026-08-21 ErrorState title 충돌 사건).
6. **미리보기** — `pnpm dev` 후 URL 안내. 마무리에 "HTML 파일로도 뽑아드릴까요?"를
   한 번 제안한다.
7. **결과물 취급** — 생성 페이지는 요청자 로컬 보관이 기본(커밋하지 않는다).
   커밋·푸시, 갤러리(`playground/public/generated/pages.json`) 등재,
   **히스토리 등재**(365 페이지 「히스토리」에 날짜+요약+내용 항목 추가 —
   관리자가 "이번 작업 히스토리로 등재해줘"라고 지시할 때만)는
   저장소 관리자(디자이너)가 지시할 때만 한다.
   **"HTML로 줘" 요청 시에만** `pnpm build`를 실행해 `playground/out/generated/<이름>/`의
   정적 HTML을 안내한다 — 서버·설치 없이 파일로 열리므로, 화면만 볼 사람에게는
   이 파일만 전달하면 된다.

## 질문 출력 형식 (터미널 가독성)

질문이 2개 이상이거나 선택지가 있으면 — 특히 don't-ask 모드의 텍스트 폴백 질문은 —
반드시 아래 구분선형 템플릿으로 출력한다:

    QUESTIONS — N개 (맨 아래 한 줄로 답해주세요)

    ── Q1. <주제> ──────────────────

    <질문 한 문장. 필요하면 링크 한 줄.>

    - **1)** <선택지> ★추천 — <반 줄 설명>
    - **2)** <선택지> — <반 줄 설명>
    - **3)** <선택지>

    ── Q2. <주제> ──────────────────

    (같은 구조 — 선택지 토큰만 a/b/c)

    ── 답변 ──────────────────────

    한 줄로: 예) `1 a 가 go`

- **질문이 먼저다** — 검토·의견(OPINION)은 질문 블록 뒤에 3줄 이내로.
- 선택지는 한 줄에 하나, 추천에는 `★추천`. 설명은 대시 뒤 반 줄.
- 토큰은 질문마다 다른 문자를 쓴다: Q1=`1/2/3`, Q2=`a/b/c`, Q3=`가/나/다` —
  한 줄 답변이 섞여도 구분되게.
- 마지막은 항상 `── 답변 ──` 블록 + 한 줄 예시. 사용자가 짧은 토큰으로 전부
  답할 수 있어야 한다.
- 유니코드 박스(┌─┐) 직접 그리기 금지 — 한글 2칸 폭 때문에 깨진다. 구분선(──)만 쓴다.

## DS 요청 (Jira 리포트)

사용자가 "DS 요청"이라고 말하거나, 생성 중 **어휘 밖 자리표시가 발생하면**
세션이 "Jira에 DS 요청으로 올릴까요?"를 제안한다. 진행 시:

1. 제목 1줄 + 본문(무엇이·왜 필요한지, 발생 화면 맥락)으로 정리해 사용자에게 확인받는다.
2. `node --use-system-ca scripts/ds-jira.mjs "<제목>" "<본문>"` 실행
   (`--type 추가|수정|버그`, 기본 추가). 요청자·DS 버전은 스크립트가 자동 첨부한다.
   **첨부는 AS-IS·TO-BE 두 장 비교가 기본** — AS-IS(발생 화면, 헤드리스 캡처)와
   TO-BE(기대안·수정안 렌더, 있으면)를 `--attach`로 각각 첨부한다. 파일명에 AS-IS/TO-BE 명시.
   **리뷰 링크를 `--link`로 포함한다** — 실제 확인 가능한 URL(배포 365 페이지 딥링크·미리보기).
   링크가 없으면 본문에 "어느 제품·어느 페이지에서 확인"인지 경로를 적는다.
3. 출력된 티켓 키·링크를 사용자에게 알려준다.

- 티켓은 Jira **DES(Product Design)** 보드로 간다 — 대상은 스크립트에 고정되어 있다.
- 필요 환경변수는 `JIRA_EMAIL`·`JIRA_API_TOKEN` 둘뿐(본인 계정). 미설정이면 스크립트가
  설정 안내를 출력한다 — 안내만 전달하고, 환경변수 값을 채팅·명령줄에 절대 노출하지 않는다.
- 반드시 `--use-system-ca`(Node 22.15+)로 실행한다(사내 TLS 검사 대응).
  실패 시 IT 보안팀 문의를 안내한다.

## 피그마 토큰 동기화 (Avikus Design library)

- 원천: 라이브러리 변수 **Theme(29종 × 제품 3모드)** · **Colors(244종)**. 저장소의
  "피그마 마지막 확인 상태"는 `dstk/figma-theme-snapshot.json`(스냅샷)이다.
  `dstk/THEME-MAP.md`는 ds:build가 스냅샷에서 자동 생성하는 대조표다.
- **검사 시점**: ① 세션 시작 ② 태스크 시작 ③ 세션 중 1시간 경과(훅이 리마인더 출력).
  검사 방법 = Figma MCP로 **대조표 노드 2807:24**(fileKey i5IhnacRAjg6NJdmtctfn2)를
  get_design_context로 읽어 텍스트 표를 파싱 → 스냅샷과 diff. (Variables REST API는
  Enterprise 전용 — 훅에서 직접 호출 불가, 검사는 Figma MCP가 연결된 세션이 수행.)
- **변경 발견 시**: 보고 → 승인 후 semantic/palette 반영 + 스냅샷 갱신을 **같은 커밋**으로.
  **무단 반영 금지** — 값 해석·매핑 판단이 필요한 변경은 질문이 먼저다.
- **빌드 게이트**: ds:build가 스냅샷 ↔ semantic·palette 해석값을 전수 대조한다 —
  불일치 = 빌드 실패(스냅샷만 고치고 시맨틱을 안 고치면, 또는 그 반대면 빌드가 막는다).

## 금지

- `dist/dstk.css`, `playground/app/dstk.css` 직접 편집 금지.
  `dstk/*.json`을 고치고 `pnpm ds:build`를 실행한다.
  **클론(비관리자)에서는 `ds:build` 실행도 금지** — dist는 커밋된 산출물을
  그대로 쓴다(로컬 변경이 생기면 자동 최신화 pull이 막힌다). 토큰 빌드·커밋은
  저장소 관리자만 한다.
- `@ds/ui`에 없는 컴포넌트 임의 생성 금지 — 필요하면 사용자에게 보고.
- **플로팅 UI(드롭다운·팝오버·셀렉트류) 직접 포지셔닝 구현 금지** — absolute 즉석 패널을
  그리지 않는다. 기본은 채택된 `DropdownMenu`·`Select`·`Popover`(포털·충돌 회피 내장)이고,
  아이콘+목록 셀렉터는 `IconSelect` 프리셋을 쓴다.

## 명령어

- `pnpm dev` — playground 미리보기 (http://localhost:3000)
- `pnpm ds:build` — 토큰 CSS 재생성
- `pnpm build` — 전체 빌드 검증

## 환경 참고

사내 보안 훅이 `TOKEN`이 포함된 셸 명령을 차단한다. 그래서 디자인 토큰 원천
디렉터리 이름은 `dstk/`이고 스크립트는 `ds:build`다. 이 명칭을 바꾸지 않는다.
